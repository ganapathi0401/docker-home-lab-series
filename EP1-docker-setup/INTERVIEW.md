# Interview Questions for EP1 - Docker Setup and Basics

1. What is the difference between an image and a container in Docker?
2. Explain the purpose of the `docker run` command.
3. What is the role of Docker Hub in the Docker ecosystem?
4. How would you remove all stopped containers in Docker?
5. What is the significance of the `--rm` flag when running a container?