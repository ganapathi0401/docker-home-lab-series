# Interview Questions for EP2 - Dockerizing a Flask App

1. What is the purpose of a `Dockerfile`?
2. Explain the significance of the `EXPOSE` instruction in a `Dockerfile`.
3. How does `.dockerignore` improve the Docker build process?
4. What is the difference between `COPY` and `ADD` in a `Dockerfile`?
5. How would you debug a container that fails to start?